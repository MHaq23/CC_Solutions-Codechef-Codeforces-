#include <iostream>
#include <string>
#include<vector>
#include<bits/stdc++.h>
#include<stdint-gcc.h>
using namespace std;
#define ll  long long int


int main() {

  int n;
  cin>>n;
  int a[n];
  cin>>a[0];
  for(int i=1;i<n;i++)
  {
    cin>>a[i];

  }
  map<int,int>m;int f=0;set<char> st;
  for(int i=0;i<n;i++)
  {
      m[s[i]]++;

  }
  for(map<int,int> iterator:: i=m.begin();i!=m.end();i++)
  {
      if(abs(it->first-(it+1)->second)==1)ct++;
        else

  }
 if(f==1)cout<<-1;
 else
    for(int i=0;i<k;i++)
  {
      for(set<char>::iterator it =st.begin();it!=st.end();it++)
      {
      cout<<*it;
      }

  }

    return 0;

}
